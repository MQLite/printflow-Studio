# PF-AUDIT-A0 — Plan

Scope: close only R1 harness-to-candidate build origin before A1/A2. Canonical master at initial HEAD 1c591e986472644446d1112462b688e9c11e022b. Tracked tree initially clean; operator-owned printflow-remediation-prompts/ remains untracked and untouched. Installed routing v2.3 read from C:/Users/admin/.codex/workflows/development-routing.md (CODEX_HOME unset in shell). SDK resolved per-user: 10.0.400.

## Implementation and acceptance

1. Preserve R1 claims, byte checks, review history, completeness, host/invocation and refusal/revocation semantics. Replace label-only origin with one controlled local pair, not an arbitrary-folder certification tool.
2. Producer creates fresh ignored output sets from committed relevant inputs with explicit SDK, locked dependencies and profiles; verifies input stability before/between/after. Failed/incomplete pairs receive no completed receipt.
3. Run checks receipt and actual loaded harness/candidate bytes after permanent claim and before operational setup. Wrapper selects the receipt's concrete test DLL without building. Explicit diagnostic unbound evidence remains nonpublishable.
4. Publication checks the original run's receipt hash/identity and both named output sets, then the current target candidate. Review preserves binding unchanged. No runtime authorization model change planned.
5. Focused synthetic consumer and producer tests first; independent read-only review; settle and commit implementation; one real local build-pair proof. Final suite scope follows actual shared Product behavior changed. No startup full suite.
6. Report, append dated R1 correction, update relevant runbook commands, HANDOFF and docs commit. Stop before A1/A2/A3.

Controlled pair chosen over executing published Product DLLs within testhost: avoids dependency/loader substitutions and preserves the current supported build profiles. Local unsigned tooling addresses accidental stale/dirty/mixed builds, not malicious rewriting by a workstation administrator.

## Routing

RouteOffset 0 (explicit user profile), AdjustmentResult UNCHANGED. Initial boundary analysis NormalRoute/RequestedRoute Astra High; ActualRoute UNVERIFIED (client does not expose current effort metadata). Post-plan implementation NormalRoute/RequestedRoute Sol High for artifact identity/failure paths. Current-context MODEL_SWITCH_UNAVAILABLE; safe parent continuation, no claimed switch. Independent producer and consumer-test subtasks use native fresh-context agents requested gpt-5.6-sol/high; ActualRoute UNVERIFIED until runtime metadata substantiates it. Context CONTINUE for cohesive implementation; FRESH_REQUIRED read-only reviewer after changes. No conversation split solely for a stage/model.

## Explicit boundaries

No real external application, workstation lease store, readiness, standard set, production revalidation, Product launch, preset/Adobe/Operator/Jira change. No R4 process stop. No branch/worktree/clone, reset/amend/rebase, push/install/deploy or attribution trailers. Local publish only generates staged files. R4 fresh-launch COM, Meitu window-loss and leftover-wrapper findings remain unchanged.

## Pre-commit verification checkpoint

Focused consumer tests: 29 passed / 0 failed / 0 skipped, Release. Producer Pester: 4 passed / 0 failed;
one redundant source-string test was removed because actual builds will verify the profiles. No behavior
assertion was weakened. Earlier producer agent run was 5/5 including that redundant check.

Fresh read-only reviewer (native subagent requested gpt-6-astra/high, ActualRoute UNVERIFIED) found two
P1s: SDK-compilable ignored nested directories omitted from inputs, and conventional stale reviewer
DLL capable of dropping the origin field. Both corrected; narrowed independent re-review found no
further actionable issue. Counterexample now uses separately compiled same-label source output and
actual pre-A0 writer acceptance/current writer refusal, with active-record preservation.

Execution remains CONTINUE. Shared Product source changed only documentation comments: no runtime
identity/schema/evaluator change, no full-suite rerun justified. Affected union and post-commit real
controlled pair are the remaining checks. R3 11,836/0/0 and R4 18/18, 758/758 remain historical only.

## Completion checkpoint

Affected union: 777 passed / 0 failed / 0 skipped; full suite NOT RUN by the scope decision above.
Code committed as 8f94f98, then producer preparation correction ce29954. The initial real attempt
refused old non-input .trx logs before building and remains incomplete; correction was behaviorally
tested (4/4) and independently re-reviewed before the next commit/build.

Real controlled pair from ce29954: ef6182db-6f48-4ed2-ae8f-c4e52307ca9e, 601 committed input identities,
182 harness files. Receipt SHA-256 E0373C56D015FED9E09D8B0F77C55B026CAE40ED3B685ABFB899709944077FBF.
Both builds exit 0, read-only receipt check exit 0, loaded paired-harness proof 1/1. All four Product
profile pairs have same labels and different bytes, accepted against their respective inventories.
Independent reviewer checked the actual files/hashes and logs, with no remaining actionable findings.

Completion: build-origin mechanism and A1's origin prerequisite verified for preserved artifacts,
subject to the explicit local trust assumptions in the report. Documentation-only closure does not
rebuild or invalidate the pair. No release acceptance earned. Stop before A1/A2/A3.
