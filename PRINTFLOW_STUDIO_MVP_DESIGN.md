# PrintFlow Studio MVP 设计文档

> **Reference translation.**
> `PRINTFLOW_STUDIO_MVP_DESIGN_EN.md` is the implementation authority if the two documents differ.
>
> **参考译本。** 若两份文档存在差异，以 `PRINTFLOW_STUDIO_MVP_DESIGN_EN.md`（英文确认版）为实现依据。

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 1.0 |
| 设计状态 | 已确认 |
| 确认日期 | 2026-08-17 |
| 平台 | Windows 固定工作站 |
| MVP 范围 | 单操作员、单图片、本地桌面流程助手 |

## 1. 文档目的

本文档是 PrintFlow Studio MVP 实现和验收的设计基准，定义产品边界、固定流程、状态和文件规则、用户界面、自动化安全约束、模块接口、本地数据设计、测试策略与验收标准。

如果实现期间发现 Meitu、Photoshop、蒙泰 RIP 或固定工作站与本文假设不一致，应先更新设计或生产环境预设，不得通过静默容错继续生产。

## 2. 产品定义

PrintFlow Studio MVP 是一个单操作员、单图片、分步骤审核的 Windows 桌面处理助手。它控制 Meitu 和 Photoshop 的现有桌面界面，减少操作员在高清化、去背景、裁边、设置尺寸和生成 DTF TIFF 之间的重复操作。

### 2.1 主要目标

1. 将操作员主动操作时间至少减少 30%。
2. 为设计素材、客户成品图和本店已完成设计提供明确、一致的处理流程。
3. 每个重要变换后允许人工审核，不静默生成错误生产文件。
4. 将第三方桌面自动化隐藏在可替换、可测试的模块后。
5. 在崩溃、未知对话框或界面变化时安全停止，并允许恢复或人工接管。

### 2.2 明确排除

MVP 不包含：

- Job、订单或客户管理；
- TeeNova 集成；
- 多图片批处理；
- 多 Artwork 或任务级联动；
- 多用户、登录、角色和权限；
- 用户可配置的通用工作流引擎；
- DTF 排版、自动嵌套、卷长计算、RIP 或打印机控制；
- 客户沟通和生产状态通知；
- 云端图片、日志、截图或用量上传；
- 通用颜色管理或替代 Photoshop 的图像处理引擎。

## 3. 核心原则

1. **一次一张图片。** 一个活动处理会话只有一个输入。
2. **不覆盖用户文件。** 原文件被快照化，第三方工具只操作工作副本。
3. **处理尝试与文件版本分开。** 只有成功导出、可读取并已计算哈希的结果才成为 Revision。
4. **重要变换后必须审核。** 高清化、去背景、裁边和最终 TIFF 均不得静默进入下一步。
5. **只在可识别状态中自动继续。** 未知界面或不可验证输出必须停止。
6. **人工接管是正常结果。** 它结束当前自动流程，操作员完成后重新拖入结果。
7. **固定生产环境优先。** MVP 只承诺在已验证的固定 Windows 工作站运行。

## 4. 用户与环境

### 4.1 用户模型

- 单操作员；
- 无登录和权限系统；
- 当前操作员同时负责审核；
- 审核记录使用 Windows 当前用户名，无法取得时使用 Operator。

### 4.2 固定工作站合同

生产环境需要记录：

- Windows 版本；
- 屏幕分辨率和缩放比例；
- Meitu 版本、界面语言和窗口布局；
- Photoshop 版本、界面语言和窗口布局；
- 已验证的 Photoshop 生产 Action；
- Photoshop 当前生产颜色设置；
- 蒙泰当前生产配置；
- 默认输出目录；
- 外部应用使用最大化还是固定窗口尺寸。

PrintFlow 本身支持中文和英文切换，但自动化只支持工作站上已验证的 Meitu 和 Photoshop 界面语言。

## 5. 核心概念

MVP 不引入 Job、Asset、Artwork 或通用 Output Variant 聚合，只使用以下概念。

### 5.1 ProcessingSession

一次单图片处理流程，包含工作类型、当前步骤、输入快照、Revision 链、处理尝试、审核和输出。

### 5.2 InputSnapshot

首次导入文件的应用管理副本。它不被 Meitu、Photoshop 或后续处理覆盖。

### 5.3 Revision

一次成功处理后已完整导出、可读取且已计算哈希的文件。Revision 记录直接来源、操作、时间、路径、哈希、像素尺寸和审核状态。

### 5.4 ProcessingAttempt

一次自动处理或人工交接尝试。Attempt 可以成功、失败、中断或被拒绝。失败 Attempt 不自动产生 Revision。

### 5.5 PrintOutput

由已审核 Revision、目标物理尺寸和固定生产预设生成的 TIFF。同一 Session 可逐个增加多个尺寸的 PrintOutput，但每次只生成一个。

## 6. 三种固定流程

### 6.1 工作类型

| 界面名称 | 内部名称 | 用途 |
| --- | --- | --- |
| 处理设计素材 | PREPARE_ASSET | 将照片、Logo 等处理为供设计使用的 PNG |
| 处理客户成品图 | PREPARE_CUSTOMER_DESIGN | 处理客户组合设计并生成生产 TIFF |
| 生成印刷 TIFF | GENERATE_PRINT_TIFF | 将本店已完成设计生成生产 TIFF |

拖入文件后必须选择工作类型。首个处理结果产生前可以返回修改类型；此后不再转换当前 Session。需要改变路线时结束当前 Session，重新拖入正确文件。

### 6.2 处理设计素材

1. 拖入一张图片并保存输入快照。
2. 原图确认。
3. 高清化，默认执行，允许跳过。
4. 人工审核。
5. 去背景，默认执行，允许跳过。
6. 人工审核。
7. 自动裁边。
8. 裁边审核或手动调整。
9. 导出合格透明 PNG。
10. 完成。

该流程不设置印刷尺寸，不生成 TIFF。

### 6.3 处理客户成品图

1. 拖入一张图片并保存输入快照。
2. 原图确认。
3. 高清化，默认执行，允许跳过。
4. 人工审核。
5. 去背景，默认执行，允许跳过。
6. 人工审核。
7. 自动裁边。
8. 裁边审核或手动调整。
9. 设置印刷尺寸。
10. Photoshop 执行生产 Action。
11. 验证 CMYK、白墨通道和 TIFF。
12. 最终审核。
13. 完成。

高清化始终先于去背景。去背景可跳过，因为完整设计可能故意保留背景。

### 6.4 生成印刷 TIFF

1. 拖入本店已完成设计并保存输入快照。
2. 设计审核。
3. 设置印刷尺寸。
4. Photoshop 执行生产 Action。
5. 验证 CMYK、白墨通道和 TIFF。
6. 最终审核。
7. 完成或新增另一个尺寸。

该流程默认不执行 Meitu 或自动裁边。如果设计未准备好，则结束当前 Session，使用其他工作类型重新导入。

### 6.5 人工处理交接

1. PrintFlow 从最近有效 Revision 创建工作副本。
2. 打开 Photoshop 或工作副本所在文件夹。
3. 当前 Session 标记为 HANDED_OFF 并结束自动流程。
4. 操作员完成处理后重新拖入结果。
5. 为新文件选择正确工作类型。

MVP 不监视 Photoshop 保存，也不自动关联人工结果与旧 Session。

## 7. 状态和审核

### 7.1 用户可见状态

| 状态 | 含义 |
| --- | --- |
| 等待处理 | 步骤尚未开始 |
| 正在处理 | 自动化正在执行 |
| 等待审核 | 已产生可验证结果 |
| 已通过 | 当前结果已通过审核 |
| 需要重试 | 当前结果不合格 |
| 已转人工 | 已结束自动流程并交接 |
| 已跳过 | 当前步骤因文件已满足而跳过 |
| 处理失败 | 未产生可验证结果 |
| 已中断 | 程序或电脑异常终止 |
| 已完成 | 当前流程所需输出已通过最终审核 |

### 7.2 转移规则

- 自动化成功并验证输出后：正在处理 → 等待审核。
- 审核合格：等待审核 → 已通过 → 下一步。
- 审核不合格：等待审核 → 需要重试。
- 重试始终从最近已审核上游 Revision 的新工作副本开始。
- 人工处理：需要重试或处理失败 → 已转人工。
- 跳过默认原因为“文件已经满足”，不产生新 Revision。
- 返回上游步骤时，下游派生结果立即失效。
- 任意时刻只允许一个 Session 持有全局自动化锁。

### 7.3 审核记录

每次审核记录：

- Session 和步骤；
- Revision 或 PrintOutput 哈希；
- 决定时间；
- Windows 用户名或 Operator；
- 合格或不合格；
- 不合格快捷原因；
- 可选备注。

快捷原因至少包括：效果不足、边缘错误、内容缺失、颜色异常、尺寸错误、白墨错误和其他。

## 8. 裁边和尺寸

### 8.1 自动裁边

- 存在透明通道时，以全部 Alpha 大于 0 的像素计算图形边界。
- 四边默认保留少量像素安全边距，初始值由真实图片测试确定。
- 用户可紧贴图形、统一增加边距、分别调整四边或取消裁边。
- 无有效透明区域时，不根据背景颜色猜测边界，进入手动裁剪。
- 自动裁边由 PrintFlow 内部确定性图像处理完成，不通过 Photoshop 界面。

### 8.2 顺序和审核

固定顺序为：去背景 → 裁边 → 裁边审核 → 设置物理尺寸 → 生成 TIFF。

裁边是独立审核步骤，显示裁边前后对比，并允许调整或撤销。

### 8.3 印刷尺寸

- 默认锁定长宽比；
- 输入宽度或高度，另一项自动计算；
- TIFF 阶段不允许非等比拉伸；
- 目标尺寸指包含安全边距的最终透明画布；
- 固定生产分辨率为 300 DPI；
- 同时显示毫米、像素、有效 DPI 和非透明图形范围；
- 有效 DPI 合格时继续，略低时警告，明显不足时阻止；
- DPI 风险阈值由真实试印确定；
- Photoshop Action 使用固定、已验证的重采样算法。

同一已审核设计可逐个增加多个尺寸 TIFF，全部使用同一份已审核源 Revision，每个输出独立生成和审核。

## 9. 输入、输出和命名

### 9.1 Meitu 流程输入

可用输入格式是 Meitu 能打开且 PrintFlow 预览模块能可靠解码的格式交集。启动 Meitu 前必须完成解码和预览验证。

MVP 不额外实现 EXIF 方向或输入色彩信息的自动标准化；显示和处理的一致性需要在固定测试集中验证。

### 9.2 生成印刷 TIFF 的输入

| 格式 | MVP 规则 |
| --- | --- |
| PNG | 支持 |
| JPG/JPEG | 支持 |
| PSD | 需包含 Photoshop 兼容合成预览；通过 Photoshop 导出扁平工作副本 |
| PDF | 仅支持单页；确认页面和目标尺寸后按生产 DPI 光栅化 |
| 多页 PDF | 不支持，不自动选取第一页 |

导入 PSD 或 PDF 时需要检测颜色模式、Alpha 和专色通道。如发现已有白墨专色通道，自动化停止，让用户选择保留现有白墨或按生产预设重新生成。

### 9.3 输出

- 设计素材流程输出透明 PNG；
- 两条生产流程输出 TIFF；
- TIFF 的 CMYK、白墨和保存选项由已验证的 Photoshop Action 与当前生产设置负责；
- 流程模块不硬编码蒙泰特定通道名称、极性、墨量、缩边或白彩顺序；
- 生产正确性依赖固定工作站中已验证的 Photoshop Action 和蒙泰环境。

### 9.4 命名

导入后显示“输出名称”字段，默认取原文件名并允许修改乱码或无意义名称。该操作不重命名用户原文件。

建议名称：

- 名称_HD.png；
- 名称_CUTOUT.png；
- 名称_280mm_CMYK_W.tif。

文件名需移除 Windows 禁止字符。目标已存在时自动添加 _02、_03 等后缀，不静默覆盖。

## 10. 文件工作区和保留策略

首次运行时设置并记住默认输出根目录。每个 Session 拥有独立子目录，逻辑上包括：

- Working：临时工作副本；
- Approved：审核通过的 PNG 和 TIFF；
- Rejected：审核期间保留的不合格派生结果。

保留和删除规则：

- 不覆盖或删除用户原文件；
- 不自动删除当前有效 InputSnapshot；
- 不自动删除已审核合格的 PNG 或 TIFF；
- 由 PrintFlow 生成且被判定不合格的 TIFF 移入 Windows 回收站；
- 不合格 Meitu 派生结果在 Session 结束前可供比较，结束后清理；
- Working 中的 Meitu 和 Photoshop 工作副本在安全结束后清理；
- 返回上游后，下游文件从当前流程失效；其中不合格 TIFF 按规则移入回收站。

最近处理页显示最近 30 天或最近 100 个 Session，取先达到的限制。超出后从列表隐藏，但不删除 Approved 生产文件。

## 11. 自动化设计

### 11.1 自动化优先级

1. Windows UI 元素；
2. 稳定快捷键；
3. 可验证的视觉识别；
4. 绝对坐标，作为最后手段。

Power Automate Desktop 可作为 MVP Adapter 的实现细节，但流程模块不得依赖 PAD 流程名、按钮、坐标或内部步骤。

### 11.2 Meitu 自动化

Meitu 处理模块负责：

- 将新工作副本导入 Meitu；
- 执行高清化或去背景；
- 等待可观察的完成状态；
- 导出到预定工作路径；
- 验证文件存在、大小稳定、可完整读取且格式合理；
- 返回已验证输出或结构化失败。

不能因为界面显示结果就创建 Revision。只有导出、读取和哈希都成功后才创建 Revision。

### 11.3 Photoshop 自动化

Photoshop 输出模块负责：

- 必要时将 PSD 或单页 PDF 准备为扁平工作副本；
- 打开已审核输入副本；
- 根据目标物理尺寸和 300 DPI 执行生产 Action；
- 使用当前已确认的颜色设置转换 CMYK；
- 由 Action 生成白墨通道；
- 按已验证选项输出 TIFF；
- 重新打开并验证输出；
- 返回 PrintOutput 或结构化失败。

### 11.4 安全起始状态

开始自动化前必须确认：

- Meitu 无未完成编辑；
- Photoshop 无未保存文档；
- 无未知模态对话框；
- 目标应用处于已验证欢迎页或空工作区；
- 当前 Session 独占全局自动化锁。

无法确认时要求用户处理，不自动关闭未知文件。

### 11.5 可观察完成条件

固定等待时间不能作为唯一继续条件，必须组合验证：

1. 目标应用出现预期完成界面；
2. 输出文件已出现；
3. 文件大小停止变化；
4. 文件可完整读取；
5. 格式、尺寸和必需元数据合理。

### 11.6 停止与人工接管

- Meitu 正在计算时，“停止”表示计算完成后停止后续自动步骤；
- 完成结果可由用户选择导入或放弃；
- 强制终止 Meitu 仅为最后手段，并明确警告；
- 人工接管不从鼠标点击序列中间恢复；
- 未知界面、未知对话框、导出失败、审核不合格或环境检查失败时提供人工接管。

## 12. Photoshop、CMYK 和白墨 TIFF

MVP 不实现通用白墨分色算法。白墨由固定工作站中已验证的 Photoshop Action 生成，CMYK 使用 Photoshop 当前已确认的生产设置。

该策略只适用于当前验证环境，不表示所有蒙泰版本、驱动、打印机或 DTF 流程具有相同通道合同。

最终 TIFF 至少验证：

- 文件存在且大小稳定；
- 文件能重新打开；
- 像素宽高符合目标毫米和 300 DPI；
- 长宽比未意外改变；
- 颜色模式为预期 CMYK；
- 预期白墨通道存在；
- 位深和压缩选项符合当前生产 Action；
- 白墨不是明显错误的空通道或异常全画布覆盖；
- 输出哈希已计算；
- 最终人工审核绑定该哈希。

白墨通道名称、类型、极性、墨量、缩边和白彩顺序属于生产 Action 配置，不由通用流程假设。

## 13. 用户界面

### 13.1 整体布局

- 左侧：固定流程和步骤状态；
- 中央：图像预览、前后对比和裁剪；
- 右侧：当前步骤参数、说明和元数据；
- 底部：合格、不合格、重试、人工处理和跳过等可用动作。

### 13.2 页面

1. 首页／拖入页：只接受一张图片；多张时明确拒绝。
2. 工作类型选择页：以大按钮选择三种流程。
3. 原图确认页：预览、原文件名、可编辑输出名称、格式和像素尺寸。
4. 统一审核页：用于高清化、去背景和裁边。
5. 尺寸设置页：宽高、像素、DPI、比例和分辨率风险。
6. TIFF 审核页：彩色图、白墨、叠加预览和生产元数据。
7. 最近处理页：缩略图、名称、类型、步骤、时间、继续和删除。
8. 设置／环境检查页：显示并验证固定工作站合同。
9. 错误详情页：错误、截图、路径和恢复动作。

### 13.3 审核交互

统一审核模块至少支持：

- 左右对比或滑块对比；
- 同步缩放和平移；
- 适应窗口、100% 和高倍放大；
- 棋盘、白色和黑色背景；
- 合格、不合格和人工处理。

TIFF 审核额外显示：

- CMYK 彩色预览；
- 白墨通道预览；
- 彩色与白墨叠加预览；
- 毫米尺寸、像素尺寸、有效 DPI、颜色设置名称和输出路径。

### 13.4 本地化

- 首次启动默认简体中文；
- 设置中可切换英文，无需重启；
- 面向用户使用生产语言，不显示 Revision、Session、Adapter 等技术词；
- 内部状态和错误代码保持稳定英文名称。

## 14. 操作环境检查

“检查操作环境”只检查，不自动修复：

- Meitu 和 Photoshop 是否安装并可启动；
- 分辨率和缩放是否符合预设；
- 输出目录是否可写；
- Photoshop 生产 Action 是否存在；
- Photoshop 颜色设置是否已由操作员确认；
- 是否存在未保存文档或未知对话框；
- 测试图片是否能完成打开和关闭。

检查失败时显示具体项目，允许退出或转人工，不允许在环境未确认时启动高风险屏幕自动化。

## 15. 错误、恢复和诊断

### 15.1 错误记录

每次失败记录：

- Session、工作类型和步骤；
- 时间和当前应用；
- 屏幕截图；
- 输入和预期输出路径；
- 结构化错误代码和中英文说明；
- 重试次数和可用恢复动作。

错误日志和截图仅在本机保留 30 天，不自动上传。

### 15.2 崩溃恢复

启动时检查未正常结束的 Attempt 和全局自动化锁。未完成 Attempt 标记为 INTERRUPTED，允许：

- 使用新工作副本从该步骤重新开始；
- 检查并导入人工保存结果；
- 放弃当前 Attempt。

不得从上次鼠标点击位置继续。

### 15.3 诊断包

诊断包只能由操作员手动导出。导出前明确显示其中的截图、日志和路径，并要求确认。

## 16. 模块设计

### 16.1 技术基线

- 当前受支持的 .NET LTS；
- WPF 桌面界面；
- SQLite 本地元数据库；
- 图片保存在本地文件系统，不作为大型 BLOB 写入 SQLite；
- 带版本号的离线安装包，不自动更新。

### 16.2 深模块与接缝

#### 流程模块

定义三条固定流程的状态、可用命令、不变量、下游失效和恢复规则。建议使用一个小接口：向当前 Session 应用用户命令，返回新的流程快照和需要执行的效果。界面不得直接修改步骤状态。

#### Meitu 处理模块

接口只接收输入文件、操作类型和工作目录，返回已验证输出或结构化失败。导入、等待、导出、截图和 UI 识别隐藏在实现中。

- 生产 Adapter：Meitu 屏幕自动化；
- 测试 Adapter：本地可预测假实现。

#### Photoshop 输出模块

接口只接收已审核输入、物理尺寸、生产预设标识和输出名称，返回已验证 PrintOutput 或结构化失败。Photoshop 界面、Action、保存和重新打开隐藏在实现中。

- 生产 Adapter：Photoshop 屏幕自动化；
- 测试 Adapter：本地可预测假实现。

#### 裁边模块

接口接收输入图片、安全边距和可选手动裁剪矩形，返回裁边文件、原始边界和最终边界。Alpha 检测、单位换算、范围检查和文件生成隐藏在实现中。

#### 文件工作区模块

统一负责创建 Session 目录、保存快照、创建工作副本、注册派生文件、生成无冲突名称、移入回收站和安全清理。其他模块不得自行拼接或删除路径。

#### 审核模块

统一提供双图对比、缩放、平移、背景切换、不合格原因和审核决定。TIFF 审核是同一模块的深入模式，不复制独立审核交互。

#### 持久化模块

在 SQLite 事务中保存 Session、Revision、Attempt、ReviewDecision、PrintOutput、AutomationLog 和 Setting。界面不得直接执行 SQL。

### 16.3 依赖与测试接缝

- 裁边和流程转移是进程内确定性逻辑，直接通过模块接口测试；
- SQLite 和文件系统使用临时数据库及临时目录进行集成测试；
- Meitu 和 Photoshop 是不可控外部依赖，分别在接缝处注入生产 Adapter 或测试 Adapter；
- 测试通过生产调用方使用的同一接口观察结果，不穿过接口断言内部点击顺序。

## 17. 本地数据设计

SQLite 只保存元数据。具体表名和字段可在实现时调整，但不得改变不变量。

### 17.1 ProcessingSession

- ID；
- 工作类型；
- 显示／输出名称；
- 当前步骤和状态；
- 创建、更新和完成时间；
- 工作区路径；
- 自动化锁信息；
- 中断和人工交接信息。

### 17.2 Revision

- ID、Session ID 和直接来源 Revision ID；
- 操作类型；
- 文件路径、格式、大小和哈希；
- 像素宽高、DPI、颜色模式和透明信息；
- 创建时间；
- 当前有效性和审核状态。

### 17.3 ProcessingAttempt

- ID、Session ID 和步骤；
- 输入 Revision ID；
- 操作和 Adapter 标识；
- 开始和结束时间；
- 成功、失败、中断或拒绝状态；
- 可空输出 Revision ID；
- 结构化失败和重试信息。

### 17.4 ReviewDecision

- ID、Session ID 和步骤；
- Revision 或 PrintOutput ID；
- 决定时间和操作员；
- 合格或不合格；
- 快捷原因和可选备注；
- 被审核文件哈希。

### 17.5 PrintOutput

- ID、Session ID 和来源 Revision ID；
- 目标宽高毫米；
- 像素宽高和 300 DPI；
- 生产预设标识；
- 输出路径、大小和哈希；
- CMYK、白墨和 TIFF 验证结果；
- 审核状态；
- 失效、回收站和删除记录。

### 17.6 AutomationLog 和 Setting

AutomationLog 保存结构化错误与截图路径。Setting 保存界面语言、输出目录、生产 DPI、安全边距、环境信息、颜色设置确认和日志保留期。

## 18. 必须保持的不变量

1. 用户原文件永不被 PrintFlow 覆盖或删除。
2. 所有审核均绑定具体文件哈希。
3. 文件发生变化后，旧审核不得自动继承。
4. 失败 Attempt 不产生可用 Revision。
5. 只有完整导出、可读取并已计算哈希的文件能进入审核。
6. 只有已审核上游 Revision 可用于最终 TIFF。
7. 任意时刻只有一个 Session 能控制 Meitu 或 Photoshop。
8. 重试始终从干净工作副本开始。
9. 下游文件失效后不得仍显示为当前生产结果。
10. 审核不合格的 PrintFlow 生成 TIFF 必须移入回收站。
11. 环境不可识别时，自动化不得猜测点击。
12. 界面不得直接修改数据库状态、删除文件或操作外部应用。

## 19. 隐私和更新

- 所有图片、快照、截图、日志和数据库只保存在本机；
- 不自动上传客户数据；
- 诊断包只允许用户确认后手动导出；
- 使用带版本号的离线安装包；
- PrintFlow 不自动更新；
- 生产工作站上的 Windows、Meitu 和 Photoshop 更新应受控；
- 升级前必须重新运行标准测试集。

## 20. 测试策略

### 20.1 模块与集成测试

- 通过流程模块接口测试状态转移、跳过、重试、失效和恢复；
- 通过裁边模块接口测试 Alpha 边界、安全边距、无透明区域和手动裁剪；
- 使用临时 SQLite 和目录测试事务、快照、命名冲突和安全清理；
- 使用假 Adapter 模拟 Meitu 和 Photoshop 成功、失败、超时、未知对话框和中断；
- 测试只断言接口可观察结果，不依赖内部点击顺序。

### 20.2 固定工作站测试集

至少包括：

- 普通 JPG 人像；
- 带发丝的复杂背景图；
- 已透明 PNG；
- 完整客户设计；
- 带兼容合成预览的 PSD；
- 单页 PDF；
- 一份实际生产成功的参考 TIFF。

必须验证三条端到端成功路径：

1. JPG 照片 → 高清化 → 去背景 → 裁边 → 合格 PNG；
2. 客户成品图 → Meitu → 裁边 → 尺寸 → Photoshop Action → 合格 TIFF；
3. PNG／JPG／PSD／单页 PDF → 尺寸 → Photoshop Action → 合格 TIFF。

每条路径还需测试：

- 审核不合格；
- 自动重试；
- 人工接管；
- 程序重启恢复；
- 未知对话框；
- 输出验证失败；
- 不合格 TIFF 移入回收站；
- 同一设计新增多个尺寸 TIFF。

## 21. MVP 验收标准

### 21.1 功能验收

- 三条固定流程均能端到端完成；
- 高清化始终先于去背景，两者均可跳过；
- 裁边可自动计算、审核、调整或取消；
- 尺寸使用锁定比例和 300 DPI；
- Photoshop Action 能生成当前蒙泰环境可用的 CMYK 白墨 TIFF；
- 每个重要步骤继续前均需审核；
- 失败、重试、人工接管和恢复均不覆盖用户原文件；
- 审核与具体文件哈希绑定；
- 不合格 PrintFlow TIFF 移入回收站；
- 未知界面不触发猜测点击。

### 21.2 效果验收

- 操作员主动操作时间至少减少 30%；
- 单图端到端总时间不高于当前人工流程；
- 标准测试集自动化成功率至少 90%；
- 不得静默生成或标记为合格的错误 TIFF。

系统记录：

- 主动操作时间；
- 自动化等待时间；
- 单图总处理时间；
- 人工接管率；
- 自动化重试率；
- 每步骤审核不合格率。

## 22. 实施前置输入

开始自动化实现前必须收集：

1. 固定工作站 Windows、分辨率和缩放信息；
2. Meitu 版本、语言及实际高清化、去背景和导出步骤；
3. Photoshop 版本、语言和生产颜色设置；
4. 已验证的 Photoshop 生产 Action；
5. 默认输出目录和命名样例；
6. 标准本地测试图片集；
7. 至少 20 个真实人工流程的时间样本；
8. 一份已经实际用于蒙泰生产的参考 TIFF。

参考 TIFF 用于确认固定环境的实际结果，不用于假设所有蒙泰环境具有相同规则。

## 23. 建议实施顺序

1. **环境基线：**收集工作站、Action、实际操作步骤和测试图片。
2. **流程骨架：**实现固定流程、Session、SQLite、文件快照和最近处理。
3. **审核与裁边：**实现对比预览、背景切换、Alpha 裁边、手动调整和审核记录。
4. **Meitu Adapter：**实现高清化、去背景、导出验证、失败截图和人工接管。
5. **Photoshop Adapter：**实现 PSD／PDF 准备、尺寸、Action、TIFF 验证和多尺寸输出。
6. **恢复与环境检查：**实现全局锁、中断恢复、环境验证和未知对话框保护。
7. **本地化与诊断：**完成中英文、日志保留、错误详情和诊断包。
8. **生产验收：**运行标准测试集、蒙泰验证和实际试印，比较处理时间与接管率。

## 24. 已接受的 MVP 风险

1. **屏幕自动化脆弱。** 通过固定环境、可观察状态、结构化失败和人工接管缓解。
2. **Photoshop 当前设置可变。** 通过生产 Action、环境确认和真实试印缓解。
3. **TIFF 与蒙泰约定不通用。** MVP 只承诺当前验证环境。
4. **输入方向和颜色标准化推迟。** 必须通过固定测试集观察一致性。
5. **人工结果不与旧 Session 自动关联。** 这是有意接受的 MVP 简化。
6. **无 Job 管理。** MVP 只验证操作自动化，不验证客户或订单流程。

