using System.Windows.Controls;

namespace PrintFlow.App.Views;

/// <summary>The session screen. No code-behind logic: every action is a bound command.</summary>
public partial class SessionScreenView : UserControl
{
    public SessionScreenView() => InitializeComponent();
}
