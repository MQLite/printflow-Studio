using System.Windows.Controls;

namespace PrintFlow.App.Views;

/// <summary>The diagnostic page uses ordinary focusable, copyable WPF controls.</summary>
public partial class ErrorDetailsView : UserControl
{
    public ErrorDetailsView() => InitializeComponent();
}
