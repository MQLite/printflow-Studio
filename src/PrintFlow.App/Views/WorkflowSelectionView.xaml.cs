using System.Windows.Controls;

namespace PrintFlow.App.Views;

/// <summary>Workflow Selection. No code-behind logic: every action is a bound command.</summary>
public partial class WorkflowSelectionView : UserControl
{
    public WorkflowSelectionView() => InitializeComponent();
}
