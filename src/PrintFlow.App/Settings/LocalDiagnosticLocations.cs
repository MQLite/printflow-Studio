namespace PrintFlow.App.Settings;

/// <summary>The resolved local diagnostic locations supplied by application composition.</summary>
public sealed record LocalDiagnosticLocations(string LocalLogLocation, string ScreenshotLocation);
